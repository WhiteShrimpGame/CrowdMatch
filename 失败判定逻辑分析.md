# 失败判定逻辑分析

> ⚠️ **旧版存档**：本文档描述的是「每帧 `Update` 检测」的旧失败判定管线，已被事件驱动重构取代（见 `GameController.TryCheckFail` / `ContainerGroup.HasPendingFrontTransition` / `ContainerGroup.consumingCount`）。内容仅作历史存档，不再反映当前实现。

> 生成日期：2026-09-10
> 范围：`GameController` / `ConveyorBeltZone` / `ConveyorBelt` / `ContainerGroup` / `ContainerItem` / `GameManager` / `GameData` / `GameState`

---

## 1. 概述

失败判定（"玩家无解，重开本关"）的核心语义只有一句话：

> **传送带被占满，且带上的每一个像素都没有同色、可匹配、未耗尽的前排容器时，判定失败。**

它由 `GameController.CheckFail()` 每帧驱动，落到 `GameManager.GameFail()` → `ReloadLevel()`，最终原地重载当前关卡（关卡序号不变、连败次数 +1）。

失败判定与胜利判定是**同一套流水线的两个出口**，两者通过一个 `_transitioning` 标志互斥，避免同帧同时触发。

---

## 2. 触发链路

```
GameController.Update()
 └─ if (GameState.IsGameStart)   // 仅在游玩态检测
     ├─ CheckWin()
     └─ CheckFail()
          └─ IsFail()?                         // 核心判定（见第 3 节）
               ├─ true  → _transitioning = true
               │          GameState.GameFail()
               │          Invoke(DoGameFail, 1.5f)   // 延迟 1.5s 过渡
               │                └─ GameManager.GameFail()
               │                     ├─ GameData.FailCount++
               │                     └─ ReloadLevel()
               │                          ├─ GameData.Init(true)     // 计数归零
               │                          └─ GameController.ReloadLevel()
               │                               ├─ _transitioning = false
               │                               ├─ GameState.GameStart()
               │                               └─ InitLevel(GameData.CurrentLevel)
               └─ false → 什么都不做，继续等下一帧
```

关键点：

- `CheckFail()` 每次只调用一次 `IsFail()`，`IsFail()` 是**纯查询**，不产生副作用。
- 触发后靠 `_transitioning = true` 锁住，1.5 秒内不会重复判定；真正重载由 `Invoke` 延迟到 `DoGameFail`。
- 重载后 `GameState` 回到 `Start`，`IsGameStart` 重新为真，下一关的胜负检测继续生效。

---

## 3. 核心判定：`GameController.IsFail()`

```csharp
private bool IsFail()
{
    if (conveyorZone == null || conveyorZone.belt == null) return false;
    if (conveyorZone.TotalSlots <= 0)                       return false;
    if (conveyorZone.OccupiedSlots < conveyorZone.TotalSlots) return false;
    if (containerGroup == null)                              return false;

    var belt = conveyorZone.belt;
    for (int i = 0; i < belt.slotCount; i++)
    {
        var pixel = belt.GetItem(i) as PixelItem;
        if (pixel == null) continue;
        if (containerGroup.HasMatchableContainerOfColor(pixel.colorId))
            return false;   // 至少一个可匹配 → 未失败
    }
    return true;
}
```

判定由四道「前置门槛」+ 一道「逐槽遍历」组成，全部满足才返回 `true`：

| 门槛 | 含义 | 不满足时 |
|---|---|---|
| `conveyorZone == null \|\| belt == null` | 没有传送带 | 直接不判失败（回退到旧的直接聚集模式，无失败概念） |
| `TotalSlots <= 0` | 传送带容量为 0 | 不判失败 |
| `OccupiedSlots < TotalSlots` | **传送带未占满** | 不判失败（还有空位，可继续进像素） |
| `containerGroup == null` | 没有容器组 | 不判失败 |

最后一步：遍历传送带每一个槽位，只要发现**任意一个**像素存在「同色可匹配容器」，立刻返回 `false`（尚未失败）。只有当**带上每个像素都没有**可匹配容器时，才返回 `true`。

> 语义等价式：`失败 ⟺ 带上所有像素的颜色集合 ∩ 可匹配容器颜色集合 = ∅`，且传送带满载。

---

## 4. "可匹配容器" 的精确定义

`HasMatchableContainerOfColor(colorId)` 是三层的链式调用：

```
HasMatchableContainerOfColor(colorId)
 └─ 遍历每一列 col
     └─ FindMatchableInColumn(col, colorId)
          └─ 在第 0 ~ (maxOpenRows-1) 排中，返回第一个满足下面全部条件的容器
```

`FindMatchableInColumn` 的单格条件：

```csharp
if (item == null || item.IsEmpty || item.isRefilling || item.colorId != colorId)
    continue;
if (!IsOpen(col, row))
    continue;
return item;   // 命中
```

`IsOpen(col, row)` 定义（见 `ContainerGroup.IsOpen`）：

```csharp
if (row >= maxOpenRows) return false;                 // 超出开放排数
for (int r = 0; r < row; r++) {
    var f = GetItem(col, r);
    if (f != null && !f.IsEmpty) return false;        // 前方还有未耗尽的车挡住
}
return true;
```

汇总一个容器"可匹配"需要**同时**满足：

1. 非空（`!IsEmpty`，即 `_remaining > 0`，还有容量）；
2. 不在补位移动中（`!isRefilling`）；
3. 颜色相同（`colorId == pixel.colorId`）；
4. 处于开放排内（`row < maxOpenRows`，默认 4 = 最前排 + 后三排）；
5. 其正前方（同一列、更靠前）**没有**未耗尽的车（即整列已"打通"到它）。

其中条件 5 是"可匹配"的关键语义：即使后排有一辆同色空余车，只要它前面还压着没装满的车，它也不算"可匹配"，因为像素必须先喂给前排。

---

## 5. 胜负判定的对称与互斥

### 5.1 胜利判定 `CheckWin()`

```csharp
if (_transitioning) return;
if (GameData.TotalPixelCount <= 0) return;
if (GameData.ClearedPixelCount >= GameData.TotalPixelCount) { ... GameWin ... }
```

- 语义：**本关所有像素都被容器消费掉**，即 `ClearedPixelCount` 追平 `TotalPixelCount`。
- 同样有 `_transitioning` 锁 + 1.5s `Invoke(DoGameWin)` 延迟。

### 5.2 对称性

| 维度 | 胜利 | 失败 |
|---|---|---|
| 触发条件 | 清空所有像素 | 传送带满载且带上无像素可匹配 |
| 状态写入 | `GameState.GameWin()` | `GameState.GameFail()` |
| 延迟过渡 | `Invoke(DoGameWin, 1.5f)` | `Invoke(DoGameFail, 1.5f)` |
| 关卡序号 | `CurrentLevel + 1` | 不变 |
| 连败计数 | `FailCount = 0` | `FailCount + 1` |
| 收敛入口 | `GameManager.GameWin()` | `GameManager.GameFail()` |
| 重载 | `ReloadLevel()` | `ReloadLevel()` |

### 5.3 互斥

两者共享 `_transitioning` 标志，任何一个先触发都会把 `_transitioning` 置 `true`，另一个在后续帧的 `Check*()` 首行即 `return`，保证**不会同局同时判胜负**。此外 `GameState` 一旦离开 `Start`，`Update` 里 `if (GameState.IsGameStart)` 也会整体挡住后续检测。

---

## 6. 计数系统（支撑判定）

### 6.1 `TotalPixelCount`（本关像素总数）

在 `GameController.InitLevel()` 末尾统计：

```
TotalPixelCount = CountPixels() + CountPipePixels()
```

- `CountPixels()`：网格 `grid` 内所有 `PixelItem` 数量。
- `CountPipePixels()`：所有管道的 `轨道格数 × 波次颜色数` 之和（管道后续还会吐出的像素也计入胜利判定）。

### 6.2 `ClearedPixelCount`（已消费像素数）

只有"像素被容器真正吸收"时才 `+1`，共两处：

1. `ContainerGroup.MovePixelToContainer`（**回退 Lerp 分支**，无空闲落点时）：
   ```csharp
   GameData.ClearedPixelCount++;
   Destroy(pixel.gameObject);
   ```
2. `ContainerItem.BoardRoutine`（**上车跳跃分支**，落定后）：
   ```csharp
   GameData.ClearedPixelCount++;
   pixel.transform.localPosition = Vector3.zero;   // 保留为乘客，不销毁
   ```

> 两处互斥：`ConsumePixel → MovePixelToContainer` 里，`TryBoardPixel` 成功则 `yield break`（走分支 2），失败才落到分支 1。不会重复计数。

### 6.3 `FailCount`（连败次数）

- 由 `GameData.FailCount` 持有，`GameFail()` 里 `++`，`GameWin()`/`PrevLevel()` 里清零。
- 当前仅作计数，尚未驱动额外 UI / 广告 / 提示逻辑，属于预留扩展点。

---

## 7. 关键设计细节与边界情况

### 7.1 失败 ≠ "解不出来"，而是"传送带死锁"

`IsFail()` 的前提是**传送带满载**。哪怕带上所有像素都暂时无法匹配，只要还有空槽，就不判失败——因为玩家还能继续从网格/管道里点出新像素上车。只有当：

1. 传送带满了（新像素进不来），
2. 带上像素都无法匹配（旧像素出不去），

两个条件同时成立，才构成**闭环死锁**：带满 → 进不来；不能匹配 → 出不去 → 槽位永不释放 → 永远带满。此时判定失败是合理的。

### 7.2 空间范围的不一致（重要）

存在两套"能不能匹配"的检查，判定口径**不完全一致**：

| 检查 | 用于 | 是否带空间范围 |
|---|---|---|
| `FindMatchableContainer(pixel, matchRangeX, matchRangeZ)` | 传送带 `ShouldLeave`（真正让像素下车） | **是**：要求容器前排与像素当前位置 X/Z 距离分别 ≤ 0.6 / 0.8 |
| `HasMatchableContainerOfColor(colorId)` | 失败判定 `IsFail` | **否**：只要任意列存在同色可匹配容器即可 |

即：**失败判定比"真正能离开"更宽松**。理论上存在一种边界：`IsFail()` 返回 `false`（存在可匹配容器），但该容器在空间上够不到（`FindMatchableContainer` 返回 `null`），像素实际永远下不了车。

在常规闭环轨道几何下（像素会循环经过所有列、会到达远侧），只要容器存在，像素总能等对齐的时机下车，因此该不一致**通常无害**；但它是两个函数口径耦合的隐患，若未来调整 `matchRangeX/Z` 或轨道形状，需同步复查。

### 7.3 Record 模式下的行为

- Record 模式下 `ShouldLeave` 走 `IsAtFarSide`（到达远侧即下车并写入序列文件），容器**不参与吸收**。
- 但 `IsFail()` 仍然调用 `HasMatchableContainerOfColor` 查容器。

后果：Record 模式里容器永远不被消耗（始终非空），因此"带满 + 带上无同色容器"才会判失败；若带满但带上有色容器，则不判失败。由于 Record 模式本质是"自动序列采集"而非可游玩对局，失败判定在此模式下意义不大，但**当前代码没有把 `recordMode` 从 `IsFail` 中旁路掉**，属于可留意的未处理分支。

### 7.4 对"容量能否凑满"的隐含假设

`IsFail()` 只看"是否存在**可匹配**容器"，不校验"该颜色的容器剩余总容量是否足够清空带上同色像素"。例如某色容器剩余 1 格、带上有 3 个同色像素，`HasMatchableContainerOfColor` 仍返回 `true`，不会判失败——这是对的（能消化 1 个就是进度），但也意味着：

> 若某颜色的总供给（网格+管道）超过该颜色容器的总容量，会出现"永远清不完、永远不判失败"的软死锁。

当前关卡生成器用 `fillToMultipleOf3` 把每种颜色补足到 3 的倍数、容器按倍数容量生成，工程上规避了这一点。若将来开放自由关卡编辑，这是失败判定之外需要额外守护的约束。

### 7.5 非传送带路径（无 `conveyorZone`）不判失败

`IsFail()` 第一道门槛就返回 `false`。即：当 `crowdBuffer` 存在但 `conveyorZone` 为空、走旧的"直接散布聚集 + `gatheredItems`"路径时，**没有失败判定**，只有胜利判定。此时容器通过 `ContainerGroup.ProcessConsumption()` 从 `gatheredItems` 吸收。

---

## 8. 潜在问题清单（按影响排序）

1. **（低风险 / 边界）** 7.2 的口径不一致：`IsFail` 用无范围的 `HasMatchableContainerOfColor`，`ShouldLeave` 用带范围的 `FindMatchableContainer`。常规轨道下无害，但属于隐性耦合。
2. **（未处理分支）** 7.3 的 `recordMode` 未被 `IsFail` 旁路：Record 模式容器不消耗，失败判定语义失真。
3. **（约束依赖）** 7.4 的"总供给 ≤ 总容量"依赖关卡生成器保证，`IsFail` 自身不校验，自由编辑关卡时可能软死锁。
4. **（次要）** `FailCount` 只计数未使用，连败表现需自行扩展（非缺陷）。
5. **（可读性）** `IsFail()` 依赖 `conveyorZone.OccupiedSlots`/`TotalSlots` 两个只读包装，而槽位遍历又直接用 `belt.GetItem` / `belt.slotCount`，两处都间接访问 `belt`，建议统一从 `conveyorZone.belt` 读取，减少跨层引用。

---

## 9. 相关代码索引

| 关注点 | 文件 | 方法 / 字段 |
|---|---|---|
| 失败触发 | `Assets/Scripts/Gameplay/GameController.cs` | `CheckFail` / `IsFail` / `DoGameFail` |
| 胜利触发 | 同上 | `CheckWin` / `DoGameWin` |
| 传送带离开判定 | `Assets/Scripts/Gameplay/ConveyorBeltZone.cs` | `ShouldLeave` / `OnLeave` / `IsAtFarSide` |
| 传送带槽位 | `Assets/Scripts/Gameplay/Conveyor/ConveyorBelt.cs` | `OccupiedCount` / `slotCount` / `GetItem` |
| 可匹配容器查询 | `Assets/Scripts/Gameplay/ContainerGroup.cs` | `HasMatchableContainerOfColor` / `FindMatchableContainer` / `FindMatchableInColumn` / `IsOpen` |
| 容器容量 | `Assets/Scripts/Gameplay/ContainerItem.cs` | `IsEmpty` / `Consume` / `Remaining` |
| 计数增减 | `Assets/Scripts/Gameplay/ContainerGroup.cs`、`ContainerItem.cs` | `ClearedPixelCount++`（两处） |
| 状态机 | `Assets/Scripts/Core/GameState.cs` | `GameFail` / `GameWin` / `IsGameStart` |
| 数据 | `Assets/Scripts/Core/GameData.cs` | `TotalPixelCount` / `ClearedPixelCount` / `FailCount` |
| 过渡裁决 | `Assets/Scripts/Core/GameManager.cs` | `GameFail` / `GameWin` / `ReloadLevel` |
